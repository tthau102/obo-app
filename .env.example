# Database Configuration
DB_HOST=mysql
DB_PORT=3306
DB_NAME=obo
DB_USERNAME=obo_user
DB_PASSWORD=your_secure_password_here

# Application Configuration
APP_PORT=8080
SPRING_PROFILES_ACTIVE=dev

# JWT Configuration
# Generate a secure secret with: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_256_BIT_KEY
JWT_DURATION=7200

# Production Database (for production profile)
DATABASE_URL=jdbc:mysql://your-db-host:3306/obo?useSSL=true&requireSSL=true&serverTimezone=UTC
DATABASE_USERNAME=obo_user
DATABASE_PASSWORD=your_production_password

# IMPORTANT SECURITY NOTES:
# 1. Never commit the actual .env file to version control
# 2. Generate strong random JWT secret: openssl rand -base64 32
# 3. Use different passwords for dev/staging/production
# 4. Rotate JWT secret periodically
# 5. Use a secret management service in production (AWS Secrets Manager, Vault, etc.)
